spring.cloud.config.uri=${CONFIG_SERVER_URI:http://localhost:8888}
spring.application.name=uj-file-uploader
spring.profiles.active=${CONFIG_SERVER_PROFILE:test}
logstash.host=${LOGSTASH_HOST:localhost:5000}